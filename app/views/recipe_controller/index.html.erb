<h1>Results for - <%= @search_term %></h1>
<% if (@recipes.nil?) || (@recipes.empty?) %>
  <p>Aw shucks, nothing found..try searching for something other than <%= @search_term %></p>

<% else %>
<table border="1">
  <tr>
    <th>Image</th>
    <th>Recipe Name</th>
    <th>Rank</th>
  </tr>
  <% @recipes.each do |recipe| %>
  <tr>
    <td><%= link_to(image_tag(recipe["image_url"], height:"100",width:"100"),recipe["f2f_url"]) %></td>
    <td><%= link_to(recipe["title"],recipe["f2f_url"]) %></td>
    <td><%= recipe["social_rank"] %></td>
  </tr>
<% end %>
  </table>
<% end %>
